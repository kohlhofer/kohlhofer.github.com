{
  "name": "kohlhofer-portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio site for Alexander Kohlhofer",
  "scripts": {
    "build": "node build.js",
    "watch": "node watch.js",
    "serve": "browser-sync start --server --files '*.html, css/*.css, js/*.js, images/**/*' --no-notify --no-open",
    "dev": "npm-run-all build --parallel watch serve",
    "start": "npm run dev"
  },
  "keywords": [
    "portfolio",
    "design",
    "product"
  ],
  "author": "Alexander Kohlhofer",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "chokidar": "^3.5.3",
    "npm-run-all": "^4.1.5"
  }
}
